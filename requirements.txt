# ============================================================
# LDCT Prediction System — Python Dependencies (Linux / Bash)
# ============================================================
# Install with:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

# --- WEB FRAMEWORK ---
Flask==3.0.0
gunicorn==21.2.0
werkzeug>=3.0.0

# --- TENSORFLOW + KERAS ---
# TF 2.16+ officially requires Keras 3.x — no version conflict
tensorflow==2.16.1
keras==3.12.1

# --- DATA SCIENCE & ML CORE ---
# numpy 1.26.x required: pkl files use numpy._core (not available in 1.24.x)
numpy==1.26.4
scipy==1.14.1
joblib==1.4.2
scikit-learn==1.6.1
# --- IMAGE PROCESSING ---
pillow>=9.0.0
scikit-image==0.24.0
PyWavelets==1.8.0
opencv-python-headless==4.10.0.84

# --- UTILITIES & API ---
python-dotenv==1.2.1
openai==2.24.0
httpx==0.27.2
